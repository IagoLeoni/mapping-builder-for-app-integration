{
  "name": "{{INTEGRATION_NAME}}",
  "updateTime": "{{UPDATE_TIME}}",
  "createTime": "{{CREATE_TIME}}",
  "triggerConfigs": [{
    "label": "API Trigger",
    "startTasks": "{{START_TASKS}}",
    "properties": {
      "Trigger name": "{{TRIGGER_NAME}}"
    },
    "triggerType": "API",
    "triggerNumber": "2",
    "triggerId": "{{TRIGGER_ID}}",
    "position": { "x": 140, "y": 45 },
    "inputVariables": {},
    "outputVariables": {
      "names": ["Output"]
    }
  }],
  "taskConfigs": "{{TASK_CONFIGS}}",
  "integrationParameters": "{{INTEGRATION_PARAMETERS}}",
  "errorCatcherConfigs": [],
  "integrationConfigParameters": "{{CONFIG_PARAMETERS}}"
}
