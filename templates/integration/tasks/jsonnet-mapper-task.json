{
  "task": "JsonnetMapperTask",
  "taskId": "{{TASK_ID}}",
  "parameters": {
    "template": {
      "key": "template",
      "value": {
        "stringValue": "{{JSONNET_TEMPLATE}}"
      }
    }
  },
  "nextTasks": "{{NEXT_TASKS}}",
  "taskExecutionStrategy": "WHEN_ALL_SUCCEED",
  "displayName": "{{DISPLAY_NAME}}",
  "externalTaskType": "NORMAL_TASK",
  "position": { "x": "{{POSITION_X}}", "y": "{{POSITION_Y}}" }
}
